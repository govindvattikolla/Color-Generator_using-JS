created a random color generator using java script

when you click on generate color button it generates a new color based on hexa color code
the color box itself also acts as a generate color button.
